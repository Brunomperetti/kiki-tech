import {useEffect,useState} from 'react';import {ProductTable} from '../components/ProductTable';import {api} from '../services/api';import type {Result,Status} from '../types/catalog';
export function Products(){const [items,setItems]=useState<Result[]>([]),[search,setSearch]=useState(''),[status,setStatus]=useState('');useEffect(()=>{const q=new URLSearchParams();if(search)q.set('search',search);if(status)q.set('status',status);const timer=setTimeout(()=>api.products(`?${q}`).then(setItems),250);return()=>clearTimeout(timer)},[search,status]);return <><header><div><p className="eyebrow">CATÁLOGO</p><h1>Productos</h1><p>Resultados normalizados y trazables.</p></div></header><div className="filters"><input placeholder="Buscar por SKU, EAN o nombre" value={search} onChange={e=>setSearch(e.target.value)}/><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">Todos los estados</option>{['ALREADY_PUBLISHED','CANDIDATE_TO_PUBLISH','REVIEW_REQUIRED','POSSIBLE_DUPLICATE','INCOMPLETE_DATA','INVALID_SKU','INVALID_EAN','UNMATCHED_ML_LISTING'].map(x=><option key={x}>{x as Status}</option>)}</select></div><ProductTable items={items}/></>}
