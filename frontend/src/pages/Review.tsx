import {useEffect,useState} from 'react';import {ProductTable} from '../components/ProductTable';import {api} from '../services/api';import type {Result} from '../types/catalog';
export function Review(){const [items,setItems]=useState<Result[]>([]);useEffect(()=>{api.products('?review_only=true').then(setItems)},[]);return <><header><div><p className="eyebrow">CONTROL HUMANO</p><h1>Revisión</h1><p>Casos ambiguos o con datos que requieren atención.</p></div></header><div className="notice warning">Ninguna coincidencia por texto genera una asociación definitiva.</div><ProductTable items={items}/></>}
