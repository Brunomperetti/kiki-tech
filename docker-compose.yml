services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: kiki
      POSTGRES_USER: kiki
      POSTGRES_PASSWORD: kiki-local
    ports: ["5432:5432"]
    volumes: ["postgres_data:/var/lib/postgresql/data"]
volumes:
  postgres_data:
